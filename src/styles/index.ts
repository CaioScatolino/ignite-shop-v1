import { createStitches } from '@stitches/react'

export const {
    config,
    styled,
    createTheme,
    globalCss,
    keyframes,
    getCssText,
    theme,
    css,
} = createStitches({

    theme: {
        colors: {
            rocketseat: '#8257e6'
        }
    }

})
